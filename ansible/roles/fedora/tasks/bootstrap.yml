---
- name: Perform yum update
  yum:
    name: '*'
    state: latest

- name: Install Git, Tmux, and other packages
  package:
    name: "{{ item }}"
    state: present
  with_items:
    - git
    - tmux
    - vim
    - htop
